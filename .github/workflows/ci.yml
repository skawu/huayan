name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.7.3'
        arch: ${{ matrix.os == 'ubuntu-latest' ? 'gcc_64' : 'win64_msvc2022_64' }}
        cache: true

    - name: Install dependencies (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libgl1-mesa-dev libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-xinerama0 libxcb-xfixes0

    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF

    - name: Build project
      run: cmake --build build --config Release --parallel

    - name: Run tests (if available)
      run: |
        if [ -f build/tests/unit/run-tests ]; then
          cd build/tests/unit && ./run-tests
        elif [ -f build/tests/run-tests ]; then
          cd build/tests && ./run-tests
        else
          echo "No tests found"
        fi
      if: matrix.os == 'ubuntu-latest'

    - name: Run tests (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        if (Test-Path "build\tests\unit\Release\run-tests.exe") {
          cd build\tests\unit\Release
          .\run-tests.exe
        } elseif (Test-Path "build\tests\Release\run-tests.exe") {
          cd build\tests\Release
          .\run-tests.exe
        } else {
          Write-Host "No tests found"
        }
      shell: pwsh

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.os }}-build
        path: build/
        retention-days: 7
