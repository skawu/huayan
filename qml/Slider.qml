import QtQuick 2.15
import Huayan.HMIControls 1.0

Item {
    // Forwarder to Huayan.HMIControls.Slider
    // Instantiate the control as the content of this file so unqualified "Slider" resolves.
    Slider { anchors.fill: parent }
}
